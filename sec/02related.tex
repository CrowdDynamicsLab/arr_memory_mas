\section{Related Work}
\label{sec:related}
\paragraph{Privacy and contextual norms in LM agents.}
ConfAIde and PrivacyLens evaluate whether language models recognize contextually appropriate disclosure norms~\citep{nissenbaum2009privacy}; AgentDAM extends this lens to autonomous web agents with data minimization~\citep{zharmagambetov2025agentdam}. These treat the agent as a single decision-making unit. We shift the unit of analysis to the \emph{handoff artifact} produced when one agent compresses upstream interaction for another, where a summarizer can preserve the sensitive fact while silently dropping the marker that constrained how it could be used.
 
\paragraph{Privacy leakage in multi-agent systems.}
AgentLeak shows violations frequently occur in internal inter-agent channels rather than user-facing outputs~\citep{yagoubi2026agentleak}; SumLeak formalizes \emph{compositional} leakage in which individually innocuous responses aggregate into a sensitive attribute~\citep{patil2025sum}; and prompt injection or environmental manipulation can elicit sensitive context at execution time~\citep{alizadeh2025simple, liao2024eia}. Our setting is non-adversarial: the sensitive information is already present at the source and is often accompanied by an explicit boundary marker. The failure we characterize is that the marker, not the fact, is disproportionately weakened or dropped in the cross-phase compression step.
 
\paragraph{Memory and inter-agent communication as channels.}
MEXTRA demonstrates black-box extraction of stored user--agent interactions~\citep{wang2025unveiling}; Prompt Infection~\citep{lee2024prompt}, MASLeak~\citep{wang2025ip}, and Agent-in-the-Middle~\citep{he2025red} show inter-agent channels can be exploited to self-replicate instructions, extract proprietary system information, or compromise an entire MAS. We adopt the same channel-level lens but study a non-adversarial failure: when one agent's output becomes another's input via summarization, what governance information survives, what is silently lost, and when does that loss become final audience-facing leakage?
 
\paragraph{Summarization fidelity.}
Abstractive summarization has long been known to drop hedges, attributions, and qualifying conditions~\citep{maynez2020faithfulness}. Our finding is the multi-agent privacy analogue: the propositions most vulnerable to compression are often those that license or constrain the propositions that survive, leaving the downstream agent with the fact but not the rule.
 
\paragraph{Communication Privacy Management.}
CPM theory models privacy as boundary work: individuals own private information, license its disclosure to specific co-owners under specific use conditions, and experience boundary turbulence when those conditions are violated~\citep{petronio1991communication, petronio2002boundaries, griffin2006first}. CPM distinguishes a fact's operational content (``Alex has a 2pm conflict'') from the rule governing its use (``shared in confidence; do not include in the final report''). In CPM terms our contribution is to identify a structural failure of MAS where operational content consistently survives while the licensing rule consistently does not. We name this failure mode \emph{summary collapse}, measure it with $\sigma$, $\sigma_{\mathrm{op}}$, and $\rho$, and show through controlled downstream tests that boundary representation determines whether context dump becomes final leakage.

% \paragraph{Privacy and contextual norms in LM agents.}
% ConfAIde and PrivacyLens evaluate whether language models recognize that contextually appropriate disclosure depends on \emph{who} is receiving information and \emph{for what purpose}, drawing on contextual integrity~\citep{nissenbaum2009privacy}. AgentDAM extends this lens to autonomous web agents and operationalizes data minimization---using sensitive information only when necessary~\citep{zharmagambetov2025agentdam}. These benchmarks primarily treat the agent as a single decision-making unit and ask whether a final action respects the relevant norm. We shift the unit of analysis to the \emph{handoff artifact} produced when one agent compresses upstream interaction for a downstream agent. In this setting, the norm violation may originate before the final speaker acts: a summarizer can preserve the sensitive fact while silently dropping the marker that constrained how the fact could be used.


% \paragraph{Privacy leakage in multi-agent systems.}
% AgentLeak shows that violations frequently occur in internal channels---inter-agent messages and shared memory---rather than in user-facing outputs~\citep{yagoubi2026agentleak}. SumLeak formalizes \emph{compositional} privacy leakage, in which individually innocuous responses can be aggregated to infer a sensitive attribute that no single agent reveals~\citep{patil2025sum}. A complementary thread shows that prompt injection and environmental manipulation can cause agents to disclose sensitive context at execution time~\citep{alizadeh2025simple, liao2024eia}. Our setting is different: the sensitive information is already present at the source and is often accompanied by an explicit boundary marker that should constrain its use. The failure we characterize is that the marker, not the fact, is disproportionately weakened or dropped in the cross-phase compression step.

% \paragraph{Memory and inter-agent communication as channels.}
% Two adjacent lines treat memory and inter-agent messaging as privacy-relevant surfaces. MEXTRA demonstrates black-box extraction of private user--agent interactions stored for future demonstrations~\citep{wang2025unveiling}. Prompt Infection~\citep{lee2024prompt}, MASLeak~\citep{wang2025ip}, and Agent-in-the-Middle~\citep{he2025red} show that inter-agent communication can be exploited to self-replicate instructions, extract proprietary system information, or compromise an entire MAS without directly attacking any individual agent. We adopt the same channel-level lens but study a non-adversarial failure mode: when one agent's output becomes another's input via summarization, what governance information is preserved, what is silently lost, and when does that loss become final audience-facing leakage?

% \paragraph{Summarization fidelity.}
% Outside the agent literature, work on abstractive summarization has long observed that compression drops hedges, attributions, and conditions that qualify what surface-level facts mean~\citep{maynez2020faithfulness}. Our finding is the multi-agent analogue, with a privacy consequence: the propositions most vulnerable to compression are often those that license or constrain the propositions that survive. The downstream agent then receives a fact without the rule that made the fact safe to use.

% \paragraph{Communication Privacy Management.}
% Our framing draws on CPM theory, which models privacy as boundary work: individuals own private information, license its disclosure to specific co-owners under specific use conditions, and experience boundary turbulence when those conditions are violated~\citep{petronio1991communication, petronio2002boundaries, griffin2006first}. CPM provides a vocabulary that distinguishes the operational content of a fact (``Alex has a 2pm conflict'') from the rule that governs its use (``shared in confidence; do not include in the final report''). Our contribution, in CPM terms, is to identify a structural failure of MAS---the cross-phase summary---in which operational content consistently survives while the licensing rule consistently does not.

% Prior work establishes that privacy can leak from agent memory~\citep{wang2025unveiling}, that multi-agent coordination creates new attack surfaces~\citep{lee2024prompt, he2025red, wang2025ip}, that compositional aggregation can synthesize sensitive attributes from benign fragments~\citep{patil2025sum}, and that environmental manipulation can induce disclosure~\citep{alizadeh2025simple, liao2024eia}. What has not been studied is the cross-phase summarization step itself as a structural source of privacy loss. We isolate this step, name its failure mode \emph{summary collapse}, measure it with $\sigma$, $\sigma_{\mathrm{op}}$, and $\rho$, and show through controlled downstream tests that boundary representation determines whether context dump becomes final leakage. This framing also clarifies why formatting-only mitigations are insufficient: safe handoff requires enforcing boundary rules, not only restating them.